# v0.4 4H Master Structure Engine Notes

Branch: `nci-story-v0.4`

## Purpose

This patch starts the first real master-story brain for the NCI x SMC Market Story Visualizer.

New file:

```text
05_PINE/01_VERSIONS/nci_smc_market_story_visualizer_v0_4_4h_master_structure_engine.pine
```

## What v0.4 adds

- Starts Slot 1 / 4H master structure classification.
- Adds preliminary master structure states:
  - bullish master structure candidate,
  - bearish master structure candidate,
  - master range candidate,
  - master transition / mixed,
  - breakout attempt above master KL DOWN,
  - breakdown attempt below master KL UP,
  - master structure building.
- Adds preliminary master anchor text:
  - last HL / KL UP area,
  - last LH / KL DOWN area,
  - range edge reaction,
  - breakout candle area,
  - breakdown candle area.
- Adds Slot 1 master KL UP / KL DOWN lines.
- Keeps mobile compact display and light theme / black font support.
- Keeps current chart timeframe KL layer.
- Keeps the 4H → 1H → 15M → 5M hierarchy.

## Important limitation

This is still a first approximation.

The structure state is still pivot-based and not yet full NCI validation.

Future versions must add:

```text
valid breakout standard
valid pullback standard
range after breakout/pullback
event anchor confirmation
fractal child explanation
NCI SD zone integration
```

## Core mind preserved

```text
4H = master story
1H explains 4H
15M explains 1H
5M explains 15M
current chart TF = working layer
NCI = structure backbone
SMC = support context
```

## Why this matters

v0.4 is the first step where the indicator begins to say:

```text
What is the master story?
Where is the master KL?
What is the current master anchor candidate?
```

This prepares the next critical build.

## Next planned build

v0.5 — Event Anchor Engine

Goals:

```text
identify pullback-start anchor
identify KL-touch anchor
identify range-edge anchor
identify breakout/breakdown anchor
make 1H start from the 4H event anchor
prepare fractal lower-timeframe explanation logic
```
