name: Ravi Trading Journal OS Screenshot Sync

on:
  workflow_dispatch:

jobs:
  screenshot-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r ravi-trading-journal-os/requirements.txt

      - name: Run screenshot sync placeholder
        run: python ravi-trading-journal-os/scripts/run_screenshot_sync.py
